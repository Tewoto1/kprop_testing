# Study side (model / tasks / training / analysis)
torch>=2.2
numpy>=1.24
matplotlib>=3.7
tqdm>=4.65

# Cumulant propagation (Mecha_preds/cumulants/kprop) + its experiments
scipy>=1.10          # exact bivariate-Gaussian ReLU covariance (owens_t, ndtr)
einops>=0.7          # tensor reshapes inside the kprop library
jaxtyping>=0.2       # shape annotations in the kprop library
opt_einsum>=3.3      # einsum backend used by the kprop library
joblib>=1.3          # FLOP-polynomial cache
pandas>=2.0          # run_comparison CSV / plotting
